# Seeded design canvas: ~2 MB of editor payload, regenerated from the
# .dc.html artboards + canvas.json via tools/seed.sh
simulxalive-atlas.html

node_modules/
__pycache__/
.DS_Store

# Generated roadmap page (rebuild: python3 tools/gen_roadmap.py)
dist/

# Pulled candidates awaiting review. Machine output, not corpus data: what
# survives review is promoted into data/ by hand (see docs/BACKEND.md).
data/ingest/
